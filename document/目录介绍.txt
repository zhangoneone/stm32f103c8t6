startcode存放了和mcu相关的代码。
stdriver是st官方的stm32f1系列的驱动，版本是3.5
bspsupport是衔接标准驱动和os的代码。里面有提供给操作系统的驱动接口。另外自己新做的驱动，也应该放在这里。提供接口供操作系统调用。
os是操作系统的代码，使用的freertos10版本的。os的移植要修改一点标准驱动。
app是用户应用。在bspsupport中实现了自己的驱动，应该在app中写应用代码，调用驱动。